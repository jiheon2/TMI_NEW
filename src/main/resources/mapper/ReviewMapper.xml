<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- JAVA와 연결할 Mapper 파일 설정 -->
<mapper namespace="kopo.poly.persistance.mapper.IReviewMapper">
    <!-- 리뷰 목록 조회 쿼리 -->
    <select id="getReviewList" parameterType="ReviewDTO" resultType="ReviewDTO">
        SELECT CONTENTS,
               CUSTOMER_ID,
               GOODS_NUMBER,
               REG_DT,
               REVIEW_NUMBER,
               SCORE,
               TRADER_ID,
               COUNT(*)
        FROM REVIEW
        WHERE #{traderId} = TRADER_ID
        ORDER BY REVIEW_NUMBER ASC
    </select>
    <select id="oneReviewList" parameterType="ReviewDTO" resultType="ReviewDTO">
        SELECT CONTENTS, CUSTOMER_ID, GOODS_NUMBER, REG_DT, REVIEW_NUMBER, SCORE, TRADER_ID, COUNT(*) AS scoreCount
        FROM REVIEW
        WHERE GOODS_NUMBER = #{goodsNumber}
        ORDER BY REVIEW_NUMBER ASC
    </select>
    <select id="getScore" parameterType="ReviewDTO" resultType="ReviewDTO">
        SELECT SCORE, COUNT(*) AS countScore
        FROM REVIEW
        WHERE GOODS_NUMBER =#{goodsNumber}
        GROUP BY SCORE
        ORDER BY SCORE ASC;
    </select>

    <!-- 리뷰 삭제 쿼리 -->
    <delete id="deleteReview" parameterType="ReviewDTO">
        DELETE
        FROM REVIEW
        WHERE REVIEW_NUMBER = #{reviewNumber}
    </delete>
</mapper>